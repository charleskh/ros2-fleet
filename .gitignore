# ROS2 / colcon build artifacts
build/
install/
log/

# Python
__pycache__/
*.py[cod]
.venv/

# Editor / OS
.vscode/
.DS_Store

# Rung 1 inference artifacts (images in, annotated out, downloaded model weights)
ros2_ws/rung1/sample*.jpg
ros2_ws/rung1/annotated_*.jpg
*.pt

# Rung 4 build artifacts — hardware-specific (built on the Orin, for the Orin), don't commit
*.onnx
*.engine

# M4 recordings — bags are large binary captures, keep them out of git
ros2_ws/bags/
